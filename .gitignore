# Dependencies — never commit these, they're huge and rebuildable
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Build artifacts you DO want to keep — do NOT ignore these:
# dist/tailwind.css   ← leave this OUT of the ignore list